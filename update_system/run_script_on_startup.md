On ubuntu desktop:

1. open "Startup Applications Preferences"
2. click "Add new"
3. in command, enter `gnome-terminal -- /PATH/TO/YOUR/SCRIPT.sh`
4. fill in `name` and `comment` as needed
5. make sure your script has permissions to be executed